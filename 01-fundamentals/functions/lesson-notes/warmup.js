/*
  Warmup Questions & Tasks
  ------------------------
  Answer the following questions and/or complete the tasks.

  - There's an error in the code below. Fix the error so that the code will run.
  - The variable names below have all kinds of casing. Change all of the variables so that they use camel case.
  - Update the code inside of the `console.log()` expression so that it uses template strings instead of concatenation. 
*/

const CAT_NAME = "Meowser";
const cat_sound = "Meow...";

const catSound_in_Japanese = "Nyaa...";
cat_sound = catSound_in_Japanese;

console.log(CAT_NAME + " says, '" + cat_sound + "'");
