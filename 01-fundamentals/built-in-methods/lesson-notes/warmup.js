/*
  Warmup Questions & Tasks
  ------------------------
  Answer the following questions and/or complete the tasks.

  - Complete the function below, as described. Do not use any built-in methods to write this function.
*/

/**
 * Returns the largest number from an array of numbers.
 * @param {number[]} numbers - An array of integers.
 * @returns {number} - The largest number.
 */
function findLargestNumber(numbers) {}

const numbers = [10, 20, -10, 0, 50];
const result = findLargestNumber(numbers);

console.log("LARGEST NUMBER:", result);
