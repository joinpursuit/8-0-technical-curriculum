/*
  Warmup Questions & Tasks
  ------------------------
  Answer the following questions and/or complete the tasks.

  - Write the function below, using the JSDoc style syntax and example as a guide. Use `console.log()` as a way to test your code.
*/

/**
 * Adds a new soda to an existing array.
 * @param {string[]} sodas - An array of sodas.
 * @param {string} newSoda - A new soda to be added.
 * @returns {string[]} A complete list of sodas.
 */
function addSoda(sodas, newSoda) {}

const example = addSoda(["Sprite"], "Pepsi");
console.log(example); //> ["Sprite", "Pepsi"]
