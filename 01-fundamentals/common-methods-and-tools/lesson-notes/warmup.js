/*
  Warmup Questions & Tasks
  ------------------------
  Answer the following questions and/or complete the tasks.

  - Write code that updates the `toolBag` variable below, so that the `nails` key points to the value 2.
  - Next, update the value of `screws` to be 8.
  - Finally, add the values of `nails` and `screws` together. Do so by accessing the `toolBag` object.
*/

const toolBag = {
  nails: 0,
  screws: 5,
};
