/*
  Warmup Questions & Tasks
  ------------------------
  Answer the following questions and/or complete the tasks.

  - The array below contains three elements: a string, a number, and another array. Update the code below so that the function runs and, when it does, it prints out the following text:

  Katherine G. Johnson, winner of at least 3 awards, passed away in the year 2020.
*/

const programmerDetails = [
  "Katherine G. Johnson",
  2020,
  [
    "Presidential Medal of Freedom",
    "NASA Group Achievement Award",
    "Congressional Gold Medal",
  ],
];

function printShortAwardBio(details) {
  const name = null;
  const year = null;
  const numberOfAwards = null;
  console.log(
    `${name}, winner of at least ${numberOfAwards} awards, passed away in the year ${year}.`
  );
}

printShortBio;
